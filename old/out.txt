=== In HTML output ===
function makePattern(name) {
    const e = escapeRe(name);
    const pre = /^\\w/.test(name) ? "\\b" : "";
    const suf = /\\w$/.test(name) ? "\\b" : "";
    return pre + e + suf;
}

// Combined regex - groups and cards together
const ALL_NAMES = [...new Set([...GROUP_SORTED, ...CARD_SORTED])].sort

Pattern for 'Star': \bStar\b
'Star' in 'start': None
'Star' in 'Star': <re.Match object; span=(0, 4), match='Star'>

Pattern for 'Era': \bEra\b
'Era' in 'obliterated': None
